# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
.idea/
.vscode/

# Local Claude Code state (launch.json is committed deliberately)
.claude/settings.local.json

# Claude Design canvas export — 111 MB of source artboards and assets.
# The converted pages in this repo are what ships; the export is reference only.
temporary/
